# Dependencies
node_modules/
npm-debug.log
yarn-error.log
win_app_dir_exe/
backend/uploads/tasks/attachments
# Runtime approval attachments, keep the folder but not the uploaded files
dc_backend/uploads/approvals/*
!dc_backend/uploads/approvals/.gitkeep
docker-compose.yml
# Ignore all files and subfolders within any 'upload' directory
**/upload/**

# Re-include subdirectories so Git tracks the directory tree
!**/upload/**/

# Re-include .gitkeep files so Git tracks empty folders
!**/upload/**/.gitkeep

# Environment variables
.env
.env.local
.env.*.local
# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build output
dist/
build/
*.log

# Temporary files
tmp/
temp/

# Database
*.db
*.sqlite
# TypeScript build cache
tsconfig.tsbuildinfo

.early.coverage
# Per-stack .env sets and database dumps written by update-deploy.sh / db.sh
deploy/env/
backups/
